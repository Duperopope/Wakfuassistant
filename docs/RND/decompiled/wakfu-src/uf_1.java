/*
 * Decompiled with CFR 0.152.
 * 
 * Could not load the following classes:
 *  com.google.common.base.Charsets
 */
import com.google.common.base.Charsets;
import java.nio.ByteBuffer;

/*
 * Renamed from uF
 */
final class uf_1 {
    private String adF = null;
    private long Vz = 0L;
    private int aaQ = 0;
    private short adG = 0;
    private final vh_1 adI = new vh_1();
    private final uy_1 adJ = new uy_1();
    final /* synthetic */ ue_2 adK;

    uf_1(ue_2 ue_22) {
        this.adK = ue_22;
    }

    public void avW() {
        this.adK.adF = this.adF;
        this.adK.Vz = this.Vz;
        this.adK.adG = this.adG;
        this.adK.adH.ZC = this.adJ.ZC;
        this.adK.adH.aaQ = this.adJ.aaQ;
        this.adK.adH.aaa = this.adJ.aaa;
        this.adK.adH.aeF = this.adJ.aeF;
        this.adK.adH.aeG = this.adJ.aeG;
        this.adK.adH.aeH = this.adJ.aeH;
        this.adK.adH.aeI = this.adJ.aeI;
        this.adK.adH.aeJ = this.adJ.aeJ;
        this.adK.adH.aeK = this.adJ.aeK;
        this.adK.adH.aeL = this.adJ.aeL;
        this.adK.adH.aeM = this.adJ.aeM;
    }

    private boolean cF(ByteBuffer byteBuffer) {
        return true;
    }

    private boolean cx(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        return true;
    }

    private boolean cG(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        return true;
    }

    private boolean cH(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        return true;
    }

    private boolean cI(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        return true;
    }

    private boolean cJ(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        return true;
    }

    private boolean cL(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        return true;
    }

    private boolean ea(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adI.b(byteBuffer, 10024001);
        return bl;
    }

    private boolean cM(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adI.b(byteBuffer, 10028000);
        return bl;
    }

    private boolean cN(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adI.b(byteBuffer, 10029000);
        return bl;
    }

    private boolean eb(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adI.b(byteBuffer, 10030002);
        return bl;
    }

    private boolean cO(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adI.b(byteBuffer, 10032003);
        return bl;
    }

    private boolean cP(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adI.b(byteBuffer, 10035004);
        return bl;
    }

    private boolean cQ(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adI.b(byteBuffer, 10035007);
        return bl;
    }

    private boolean cR(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.aaQ = byteBuffer.getInt();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adI.b(byteBuffer, 10036004);
        return bl;
    }

    private boolean ec(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adJ.b(byteBuffer, 10036008);
        return bl;
    }

    private boolean cS(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adJ.b(byteBuffer, 10037001);
        return bl;
    }

    private boolean cT(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adJ.b(byteBuffer, 10040005);
        return bl;
    }

    private boolean cU(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adJ.b(byteBuffer, 10045010);
        return bl;
    }

    private boolean cC(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adJ.b(byteBuffer, 10048000);
        return bl;
    }

    private boolean cV(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adJ.b(byteBuffer, 10055001);
        return bl;
    }

    private boolean cW(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adJ.b(byteBuffer, 10064001);
        return bl;
    }

    private boolean cX(ByteBuffer byteBuffer) {
        int n = byteBuffer.getShort() & 0xFFFF;
        byte[] byArray = new byte[n];
        byteBuffer.get(byArray);
        this.adF = new String(byArray, Charsets.UTF_8);
        this.Vz = byteBuffer.getLong();
        this.adG = byteBuffer.getShort();
        boolean bl = this.adJ.b(byteBuffer, 10066001);
        return bl;
    }

    public boolean b(ByteBuffer byteBuffer, int n) {
        if (n < 0) {
            return false;
        }
        if (n < 1) {
            boolean bl = this.cF(byteBuffer);
            return bl;
        }
        if (n < 313) {
            boolean bl = this.cx(byteBuffer);
            return bl;
        }
        if (n < 314) {
            boolean bl = this.cG(byteBuffer);
            return bl;
        }
        if (n < 315) {
            boolean bl = this.cH(byteBuffer);
            return bl;
        }
        if (n < 10003) {
            boolean bl = this.cI(byteBuffer);
            return bl;
        }
        if (n < 10023) {
            boolean bl = this.cJ(byteBuffer);
            return bl;
        }
        if (n < 10024001) {
            boolean bl = this.cL(byteBuffer);
            return bl;
        }
        if (n < 10028000) {
            boolean bl = this.ea(byteBuffer);
            return bl;
        }
        if (n < 10029000) {
            boolean bl = this.cM(byteBuffer);
            return bl;
        }
        if (n < 10030002) {
            boolean bl = this.cN(byteBuffer);
            return bl;
        }
        if (n < 10032003) {
            boolean bl = this.eb(byteBuffer);
            return bl;
        }
        if (n < 10035004) {
            boolean bl = this.cO(byteBuffer);
            return bl;
        }
        if (n < 10035007) {
            boolean bl = this.cP(byteBuffer);
            return bl;
        }
        if (n < 10036004) {
            boolean bl = this.cQ(byteBuffer);
            return bl;
        }
        if (n < 10036008) {
            boolean bl = this.cR(byteBuffer);
            return bl;
        }
        if (n < 10037001) {
            boolean bl = this.ec(byteBuffer);
            return bl;
        }
        if (n < 10040005) {
            boolean bl = this.cS(byteBuffer);
            return bl;
        }
        if (n < 10045010) {
            boolean bl = this.cT(byteBuffer);
            return bl;
        }
        if (n < 10048000) {
            boolean bl = this.cU(byteBuffer);
            return bl;
        }
        if (n < 10055001) {
            boolean bl = this.cC(byteBuffer);
            return bl;
        }
        if (n < 10064001) {
            boolean bl = this.cV(byteBuffer);
            return bl;
        }
        if (n < 10066001) {
            boolean bl = this.cW(byteBuffer);
            return bl;
        }
        if (n < 10069001) {
            boolean bl = this.cX(byteBuffer);
            return bl;
        }
        return false;
    }
}

