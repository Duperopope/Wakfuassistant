/*
 * Decompiled with CFR 0.152.
 */
import java.nio.ByteBuffer;

/*
 * Renamed from vZ
 */
final class vz_2 {
    private final uy_1 agC = new uy_1();
    private short agA = 1;
    private int agB = 0;
    final /* synthetic */ vy_2 agD;

    vz_2(vy_2 vy_22) {
        this.agD = vy_22;
    }

    public void avW() {
        this.agD.agz.ZC = this.agC.ZC;
        this.agD.agz.aaQ = this.agC.aaQ;
        this.agD.agz.aaa = this.agC.aaa;
        this.agD.agz.aeF = this.agC.aeF;
        this.agD.agz.aeG = this.agC.aeG;
        this.agD.agz.aeH = this.agC.aeH;
        this.agD.agz.aeI = this.agC.aeI;
        this.agD.agz.aeJ = this.agC.aeJ;
        this.agD.agz.aeK = this.agC.aeK;
        this.agD.agz.aeL = this.agC.aeL;
        this.agD.agz.aeM = this.agC.aeM;
        this.agD.agA = this.agA;
        this.agD.agB = this.agB;
    }

    private boolean cF(ByteBuffer byteBuffer) {
        return true;
    }

    private boolean cx(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 1);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cG(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 313);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cH(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 314);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cI(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 315);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cJ(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10003);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cL(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10023);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cM(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10028000);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cN(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10029000);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cO(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10032003);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cP(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10035004);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cQ(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10035007);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cR(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10036004);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cS(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10037001);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cT(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10040005);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cU(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10045010);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cC(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10048000);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cV(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10055001);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cW(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10064001);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    private boolean cX(ByteBuffer byteBuffer) {
        boolean bl = this.agC.b(byteBuffer, 10066001);
        if (!bl) {
            return false;
        }
        this.agA = byteBuffer.getShort();
        this.agB = byteBuffer.getInt();
        return true;
    }

    public boolean b(ByteBuffer byteBuffer, int n) {
        if (n < 0) {
            return false;
        }
        if (n < 1) {
            boolean bl = this.cF(byteBuffer);
            return bl;
        }
        if (n < 313) {
            boolean bl = this.cx(byteBuffer);
            return bl;
        }
        if (n < 314) {
            boolean bl = this.cG(byteBuffer);
            return bl;
        }
        if (n < 315) {
            boolean bl = this.cH(byteBuffer);
            return bl;
        }
        if (n < 10003) {
            boolean bl = this.cI(byteBuffer);
            return bl;
        }
        if (n < 10023) {
            boolean bl = this.cJ(byteBuffer);
            return bl;
        }
        if (n < 10028000) {
            boolean bl = this.cL(byteBuffer);
            return bl;
        }
        if (n < 10029000) {
            boolean bl = this.cM(byteBuffer);
            return bl;
        }
        if (n < 10032003) {
            boolean bl = this.cN(byteBuffer);
            return bl;
        }
        if (n < 10035004) {
            boolean bl = this.cO(byteBuffer);
            return bl;
        }
        if (n < 10035007) {
            boolean bl = this.cP(byteBuffer);
            return bl;
        }
        if (n < 10036004) {
            boolean bl = this.cQ(byteBuffer);
            return bl;
        }
        if (n < 10037001) {
            boolean bl = this.cR(byteBuffer);
            return bl;
        }
        if (n < 10040005) {
            boolean bl = this.cS(byteBuffer);
            return bl;
        }
        if (n < 10045010) {
            boolean bl = this.cT(byteBuffer);
            return bl;
        }
        if (n < 10048000) {
            boolean bl = this.cU(byteBuffer);
            return bl;
        }
        if (n < 10055001) {
            boolean bl = this.cC(byteBuffer);
            return bl;
        }
        if (n < 10064001) {
            boolean bl = this.cV(byteBuffer);
            return bl;
        }
        if (n < 10066001) {
            boolean bl = this.cW(byteBuffer);
            return bl;
        }
        if (n < 10069001) {
            boolean bl = this.cX(byteBuffer);
            return bl;
        }
        return false;
    }
}

